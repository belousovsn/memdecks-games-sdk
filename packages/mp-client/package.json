{
  "name": "@memdecks/mp-client",
  "version": "0.1.0",
  "description": "Browser-side helper for memdecks games: the translator:init bridge, authenticated match join over Socket.IO, and the single-player card fetch.",
  "license": "MIT",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "prepublishOnly": "npm run build"
  },
  "dependencies": {
    "@memdecks/mp-types": "^0.1.0",
    "socket.io-client": "^4.7.2"
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  },
  "publishConfig": {
    "access": "public"
  }
}
